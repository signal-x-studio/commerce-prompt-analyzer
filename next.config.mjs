/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    turbopack: {
      root: "/Users/nino/Workspace/dev/wip/commerce-prompt-analyzer",
    },
  },
};

export default nextConfig;
